const getters = {
  token: state => state.token,
  user: state => state.user,
  selectedPart: state => state.selectedPart,
  selectedSupplier: state => state.selectedSupplier,
  selectedPurchase: state => state.selectedPurchase,
  purTransTasks: state => state.purTransTasks
}

export default getters
